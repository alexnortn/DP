using UnityEngine;
using System.Collections;

public class Swarm_Color : MonoBehaviour {

	// public bool flockSize;
	// public Renderer rendCube;
	// public Color SwarmCol = Color.red;
 //    public Color FlockCol = Color.blue;

 //    public GameObject Swarm;

	// Use this for initialization
	void Start ()
	{
		// rendCube = GetComponent<Renderer>();
		// flockSize = Swarm.GetComponent<SwarmBehavior>().flockSize;
	}
	
	// Update is called once per frame
	void Update ()
	{
		 // if(flockSize)
		 // {
		 // 	rendCube.material.color = SwarmCol;
		 // }
		 // else 
		 // {
		 // 	rendCube.material.color = FlockCol;
		 // }

	}
}
